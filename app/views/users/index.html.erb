<h1> Users </h1>

<table>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td> <%= user.handle %> (<%= user.name %>) <td>
        <td> <%= button_to 'Delete', user_path(user), method: :delete %> </td>
      </tr>
    <% end %>
  </tbody>
</table>